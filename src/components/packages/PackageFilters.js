import React, { Component } from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

class PackageFilters extends Component {
  render() {
    return (
      <div />
    );
  }
}

export default PackageFilters;
